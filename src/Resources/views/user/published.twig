{% extends "skeleton.twig" %}

{% block extend_head %}
    <style type="text/css">
        #published {
            width: 100%;
            height: 100%;
            clear: both;
        }
        .post {
            display: inline-block;
            width: 30%;
            margin-right: 1%;
            margin-bottom: 10px;
            border: 1px solid #eee;
            padding: 10px;
        }
        .post div {
            margin-bottom: 5px;
        }
    </style>
{% endblock %}

{% block title %}{{ h1 }}{% endblock %}

{% block content %}
    <div id="published">
    {% for post in posts %}
        <div class="post">
            <div class="title">{{ post.title }}</div>
            <div class="ingress">{{ post.ingress }}</div>
            <div class="content">{{ post.content }}</div>
            <div class="btn float_right"><a href="/post/view/{{ post.id }}">Läs mer</a></div>
        </div>
    {% endfor %}
    </div>
{% endblock %}